{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Run",
			"command": "C:\\Program Files\\processing-4.0b7\\processing-java.exe",
			"type": "process",
			"args": [
				"--force",
				// "--sketch=${workspaceFolder}\\${relativeFileDirname}",
				// "--sketch=${workspaceFolder}\\${relativeFile}",
				"--sketch=${fileDirname}",
				// "--sketch=${file}",
				"--run"
			],
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"presentation": {
				"reveal": "always",
				"panel": "new"
			}
		}
	]
}