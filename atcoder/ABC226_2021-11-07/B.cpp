#include <bits/stdc++.h>
using namespace std;
#define _USE_MATH_DEFINES
#define ll long long int

// コンパイル時に
// -lgmpxx -lgmp
// int => mpz_class

int main(int argc, char **argv) {
	int n;
	cin >> n;
	int l[n];
	int a[n][200005];
	char str[n][]

	for (int i = 0; i < n; i++) {
		cin >> l[i];
		for (int j = 0; j < l[i]; j++) {
			cin >> a[i][j];
		}
	}

	return 0;
}
