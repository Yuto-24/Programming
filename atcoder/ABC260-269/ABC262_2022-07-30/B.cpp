#include <bits/stdc++.h>
using namespace std;
#define _USE_MATH_DEFINES
#define ll long long int

int main() {
	int n, m;
	int u[50000], v[50000];
	// input
	cin >> n >> m;
	for (int i = 0; i < m; i++) {
		cin >> u[i] >> v[i];
	}

	// define a, b, c
}