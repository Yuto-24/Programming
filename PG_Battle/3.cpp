#include <bits/stdc++.h>
using namespace std;
#define _USE_MATH_DEFINES
#define ll long long int

how_many(int a, int b) {
	// (a + b)! / (a! + b!)
}

int main(int argc, char **argv) {
	int n, m, a, b;
	cin >> n >> m >> a >> b;
	int ignore = 0;
	if (a > n || b > m) {
		ignore = 1;
	}

	return 0;
}
